const {
  configPath,
  migrationsPath,
  modelsPath,
  seedersPath,
} = require('./src/constants');

module.exports = {
  config: configPath,
  'models-path': modelsPath,
  'seeders-path': seedersPath,
  'migrations-path': migrationsPath,
};